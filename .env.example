# Copy to .env and fill in. The app works without any keys (OpenFreeMap
# basemap); add a MapTiler key for a far more detailed basemap.

# https://cloud.maptiler.com/account/keys  (free tier is fine)
MAPTILER_KEY=

# Server port (optional)
# PORT=8787
